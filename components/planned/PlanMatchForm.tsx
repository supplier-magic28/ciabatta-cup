"use client";
import { useState, useTransition } from "react";
import { useRouter } from "next/navigation";
import { planMatch } from "@/lib/planned/actions";
import { Button } from "@/components/ui/Button";
export function PlanMatchForm({ players, externals }: { players:{id:string;name:string}[]; externals:{id:string;name:string}[] }) { const [opponent,setOpponent]=useState("");const [when,setWhen]=useState("");const [location,setLocation]=useState("");const [error,setError]=useState("");const [pending,start]=useTransition();const router=useRouter();const external=opponent.startsWith("x:");const submit=()=>start(async()=>{const result=await planMatch({opponentPlayerId:external?undefined:opponent.slice(2),opponentExternalId:external?opponent.slice(2):undefined,scheduledAt:when?new Date(when).toISOString():"",location});if(result.ok&&result.id)router.push(`/matches/${result.id}`);else setError("error" in result?result.error:"Couldn't plan match.");});return <div className="border-2 border-ink bg-surface p-6 shadow-[3px_3px_0_var(--color-ink)]"><p className="font-mono text-[10px] uppercase tracking-[2px] text-muted">Nothing to decide yet</p><h1 className="mt-1 font-heading text-2xl font-bold">Plan a match</h1><div className="mt-5 grid gap-3"><p className="font-mono text-[10px] uppercase text-muted">Opponent</p>{players.map(p=><button key={p.id} onClick={()=>setOpponent(`p:${p.id}`)} className={`border-2 border-ink p-3 text-left ${opponent===`p:${p.id}`?"bg-green text-cream":"bg-cream"}`}>{p.name}</button>)}{externals.map(p=><button key={p.id} onClick={()=>setOpponent(`x:${p.id}`)} className={`border-2 border-dashed border-green p-3 text-left ${opponent===`x:${p.id}`?"bg-green text-cream":"bg-cream"}`}>{p.name} · Non-Ciabatta</button>)}<label className="font-mono text-[10px] uppercase text-muted">When<input type="datetime-local" value={when} onChange={e=>setWhen(e.target.value)} className="mt-2 w-full border-2 border-ink bg-cream p-3 font-body"/></label><label className="font-mono text-[10px] uppercase text-muted">Where<input value={location} onChange={e=>setLocation(e.target.value)} className="mt-2 w-full border-2 border-ink bg-cream p-3 font-body"/></label>{error&&<p className="font-mono text-xs text-rust">{error}</p>}<Button loading={pending} onClick={submit}>Send it</Button><p className="font-mono text-[9px] uppercase text-muted">Non-Ciabatta plans lock in immediately.</p></div></div>; }
