const esc=(s:string)=>s.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]!);
export function plannedEmail(input:{kind:"locked"|"confirmed";firstName:string;opponentName?:string;winnerName?:string;loserName?:string;when?:string;location?:string;score?:string;type?:string}){const locked=input.kind==="locked";const title=locked?"Match locked in":"Result confirmed";const body=locked?`I have locked in your match with ${input.opponentName}. ${input.when} · ${input.location}. Stakes remain undecided until you play.`:`${input.winnerName} def. ${input.loserName} · ${input.score}. ${input.type}.`;return {subject:`Ciabatta Cup: ${title}`,text:`${input.firstName}, ${body}\n\n— ZEUS APPROVES`,html:`<!doctype html><html><body style="background:#EFE6D0;color:#1B1A16;font-family:Arial,sans-serif"><main style="max-width:600px;margin:24px auto;border:2px solid #1B1A16;background:#F7F0DE"><header style="background:#1B1A16;color:#C9DA5A;padding:28px;text-align:center;font-family:monospace">${esc(title.toUpperCase())}</header><section style="padding:28px"><h1>${esc(input.firstName)},</h1><p>${esc(body)}</p><p style="margin-top:28px;color:#3E6B35;font-family:monospace">— ZEUS APPROVES</p></section></main></body></html>`};}
